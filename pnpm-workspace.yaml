packages:
  - "apps/*"
  - "packages/*"

# esbuild ships a platform binary via a postinstall script; apps/api's build
# needs it. pnpm blocks dependency build scripts unless they are allowed here.
allowBuilds:
  esbuild: true
